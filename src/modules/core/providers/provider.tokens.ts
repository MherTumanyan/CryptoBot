// TODO something wrong it doesnt work

const MongooseSDKProviderToken = Symbol('MongooseSDKProviderToken');

const EnvProviderToken = Symbol('EnvProviderToken');

const LogerProviderToken = Symbol('LogerProviderToken');

export { MongooseSDKProviderToken, EnvProviderToken, LogerProviderToken };
