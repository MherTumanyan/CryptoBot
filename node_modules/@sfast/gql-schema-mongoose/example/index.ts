import { importSchema } from 'graphql-import';
import gql from 'graphql-tag';
import { GraphEntityParser } from '@sfast/gql-schema-parser';
import { loadSchema } from 'graphql-toolkit';
// const typeDefs = importSchema('./schema.graphql');
// console.log("typeDefs", typeDefs);

// const schema = gql(typeDefs);

// const parseSchema = new GraphEntityParser(schema);
// const entities = parseSchema.entities;
// console.log('entities: ', entities);

(async () => {
	const schema = await loadSchema('schema.graphql'); // load from multiple files using glob
	console.log('schema', schema);
})();
