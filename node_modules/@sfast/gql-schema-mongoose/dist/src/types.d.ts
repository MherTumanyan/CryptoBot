import { Schema, SchemaType } from 'mongoose';
import { IEntityDefinition, IEntitySchema } from '@sfast/gql-schema-parser';
export interface MongooseEntityDefinition extends Exclude<IEntityDefinition, 'schema'> {
    schema: Record<string, any>;
    mongooseSchema?: Schema;
}
export interface SchemaTypeWithOptions extends SchemaType {
    options: any;
}
export declare type MongooseSchemaFieldType = string | IEntitySchema | Array<string | IEntitySchema>;
