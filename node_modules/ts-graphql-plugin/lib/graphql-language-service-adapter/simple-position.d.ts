import { Position } from 'graphql-language-service-types';
export declare class SimplePosition implements Position {
    line: number;
    character: number;
    constructor(lc: ts.LineAndCharacter);
    lessThanOrEqualTo(p: Position): boolean;
}
