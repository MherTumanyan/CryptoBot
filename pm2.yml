apps:
  - name        : spot_wallet
    interpreter : /bin/bash
    script      : yarn
    log_date_format : YYYY-MM-DD HH:mm:ss
    args        : [start:backend]
    env_prod:
      NODE_ENV: prod
    env_dev:
      NODE_ENV: dev
